@model MoneyTransformer.Models.Userlogin

@{
    ViewData["Title"] = "Sign In";
    Layout = "/Views/Shared/_AuthLayout.cshtml";
}
<div class="d-flex align-items-center justify-content-center">
    <p class="fs-4 mb-0 fw-bold">New to MoneyMove?</p>
    <a class="text-primary fw-bold ms-2" href="/Authentication/Register">Create an account</a>
    </div>
     
 <form method="POST" class="register-form" id="login-form" asp-action="Login">

    <div class="mb-3">
      
    <label  asp-for="Email" class="mb-0 fw-bold">Email</label>
    <input  asp-for="Email" type="email" class="form-control" id="Email" aria-describedby="emailHelp"placeholder="Email"required>
    </div>
    <div class="mb-4">
     
    <label  asp-for="Passwordd" class="mb-0 fw-bold">Password</label>
    <input  asp-for="Passwordd" type="password" class="form-control" id="Passwordd" placeholder="Password"  required>
    </div>
   @* <div class="d-flex align-items-center justify-content-between mb-4">
    <div class="form-check">
        <input class="form-check-input primary" type="checkbox" value="" id="flexCheckChecked" checked>
        <label class="form-check-label text-dark" for="flexCheckChecked">
        Remeber this Device
        </label>
    </div>
    <a class="text-primary fw-bold" href="./index.html">Forgot Password ?</a>
    </div>*@
    <div style="color:red;">
        @if (ViewData["Messag"] != null)
        {
            @ViewData["Messag"]
        }
    </div>
    
   <div class="form-group form-button">
      <input type="submit" name="signup" id="signup" class="form-submit  w-100 py-8 fs-4 mb-4 rounded-2"style="background-color: #7679ee;color: white;border-color: #7679ee;" value="Sign In"/>
   </div>
</form>
  <div class="d-flex align-items-center justify-content-center">
           <p class="fs-4 mb-0 fw-bold">Back</p>
     <a class="text-primary fw-bold ms-2" href="/Home/Index">Home</a>

       </div>
@*
<h1>Login</h1>

<h4>Userlogin</h4>
<hr />
<div class="row">
    <div class="col-md-4">
         <form method="POST" class="register-form" id="login-form" asp-action="Login">
                           <div class="form-group">
                                <label asp-for="Email" class="control-label"><i class="zmdi zmdi-account material-icons-name"></i></label>
                                <input asp-for="Email" placeholder="Email"required/>
                                <span asp-validation-for="Email" ></span>
                            </div>
                            <div class="form-group">
                                <label asp-for="Passwordd" class="control-label"><i class="zmdi zmdi-lock"></i></label>
                                <input asp-for="Passwordd" type="password" placeholder="Password"  required/>
                                <span asp-validation-for="Passwordd" ></span>
                            </div>

                            <div class="form-group form-button">
                                <input type="submit" name="signin" id="signin" class="form-submit" value="Log in"style=" background-color: #debf63"/>
                            </div>
                            <div style="color:red;">
                @if (ViewData["Messag"] != null)
                {
                    @ViewData["Messag"]
                }
                            </div>
                        </form>
    </div>
</div>
*@


